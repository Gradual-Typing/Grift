(letrec ([add1 : Dyn (lambda ((x : Dyn)) (+ x 1))])
  (let ([iters : Dyn (read-int)] [acc : Dyn (gbox 0)])
    (begin
      (timer-start)
        (repeat (i 0 iters) (gbox-set! acc (add1 (gunbox acc))))
        (timer-stop)
        (timer-report)
        (gunbox acc))))
